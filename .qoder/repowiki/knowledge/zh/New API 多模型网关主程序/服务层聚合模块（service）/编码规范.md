- 对外能力以包级函数形式暴露，构造函数通过 NewXxx(baseURL, apiKey string) 并从环境变量回退默认值
- 错误统一使用 types.NewAPIError 包装并携带 StatusCode，便于 ShouldDisableChannel/ShouldEnableChannel 等决策逻辑判断
- 日志通过 common.SysLog 输出，敏感信息经 LocalLogPreview 脱敏后再记录
- 配置集中通过 GetXxxConfig() 工厂函数返回结构体，避免全局变量散落
- 跨子模块协作通过直接调用同包函数完成，不引入额外接口抽象