所有任务均在 master 节点生效（`common.IsMasterNode` 判断），通过 `StartSystemTaskRunner()`、`StartSubscriptionQuotaResetTask()`、`StartCodexAutoRefreshTask()` 在应用启动时触发；日志清理任务需显式调用 `StartLogCleanupTask(targetTimestamp)` 创建一次性任务。