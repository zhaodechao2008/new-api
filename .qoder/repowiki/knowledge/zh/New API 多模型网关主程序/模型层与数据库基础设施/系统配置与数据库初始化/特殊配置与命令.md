启动时需依次调用 `InitDB()`、`InitLogDB()`、`InitOptionMap()`；可通过环境变量 `SQL_DSN`、`LOG_SQL_DSN`、`SQL_MAX_IDLE_CONNS`、`SQL_MAX_OPEN_CONNS`、`SQL_MAX_LIFETIME`、`LOG_SQL_CLICKHOUSE_TTL_DAYS` 控制数据库行为；MySQL 模式会强制检查字符集/排序规则是否支持中文，否则 panic。