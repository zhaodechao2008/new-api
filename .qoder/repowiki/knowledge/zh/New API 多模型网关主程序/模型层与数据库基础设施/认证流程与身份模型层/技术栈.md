GORM 作为 ORM；`github.com/go-webauthn/webauthn` 处理 Passkey/WebAuthn 协议；HMAC 签名通过 `common.GenerateHMACWithKey` 生成；TOTP 与备用码校验委托 `common.ValidateTOTPCode`、`common.HashBackupCode`、`common.ValidatePasswordAndHash`。