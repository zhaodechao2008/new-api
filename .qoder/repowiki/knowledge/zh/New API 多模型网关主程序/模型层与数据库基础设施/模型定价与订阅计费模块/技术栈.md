Go + GORM（SQLite/PostgreSQL 双数据库支持，通过 `common.UsingMainDatabase` 区分方言）；缓存层使用 `github.com/samber/hot` 作为内存 LRU 与 `github.com/QuantumNous/new-api/pkg/cachex.HybridCache` 组合 Redis；金额计算使用 `github.com/shopspring/decimal` 保证精度；测试依赖 `testify` 与 `glebarez/sqlite`。