该模块为纯文档仓库，按用途划分为四个子目录：`channel/` 存放渠道额外设置的 JSON 配置说明；`installation/` 提供宝塔面板 Docker 部署教程；`openapi/` 包含 `api.json`（后台管理接口）和 `relay.json`（Relay 接口）两份 OpenAPI 3.0.1 规范；根目录放置认证流程、IONET 客户端示例及法语/俄语翻译术语表。各 Markdown 文件相互独立，无代码依赖，仅通过相对路径引用 `images/` 下的截图资源。