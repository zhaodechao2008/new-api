开发需三端并行：`go run main.go`（后端 3000 端口）、`make dev-web`（前端 5173 端口）、`cd electron && npm run dev-app`；生产构建统一走 `./build.sh`，内部根据 `$OSTYPE` 调用 `CGO_ENABLED=1 go build -ldflags="-s -w"` 编译 Go 二进制，再执行对应平台的 `npm run build:mac|win|linux`，产物输出到 `electron/dist/`。